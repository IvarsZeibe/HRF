<UserControl x:Class="win.WritingTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:win"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <RichTextBox Grid.Row="0" Background="#222233"
                     Foreground="White" Cursor="Arrow"
                     IsHitTestVisible="False" IsReadOnlyCaretVisible="True"
                     IsReadOnly="True" x:Name="Words" />
        <TextBlock Grid.Row="1" x:Name="result" Foreground="White" />
        <TextBlock Grid.Row="2" x:Name="TimerUI" Foreground="White" />
    </Grid>
</UserControl>
